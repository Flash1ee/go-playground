# Практика
Написать
- **Dockerfile** который запустит простое скомпилированное приложение на Go
- **Dockerfile** который который сделает сборку приложения на *Go* и запустит его
- **Dockerfile** который запустит сборку приложения на *Go* в одном слое и скопирует в новый слой приложения для запуска так, чтобы в итоговом контейнере не было установленного Go, который использовался для сборки и итоговый контейнер был минималистичен(scratch, alpine)